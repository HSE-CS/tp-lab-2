#include <iostream>
#include "task3.h"

using namespace std;

int main()
{
    /*const size_t size = 5;
    int arr_int[size] = { 60, 83, 13, 76, 40 };
    double arr_dob[size] = { 6.079, 8.3, 1.3, 7.6, 4.09 };
    char arr_char[size] = { '7', '5', '4', '1', '3' };

    cout << "Initial array: ";
    for (int i = 0; i < size; i++)
        cout << arr_int[i] << " ";
    map<int, size>(arr_int, change);
    cout << "\nChanged array: ";
    for (int i = 0; i < size; i++)
        cout << arr_int[i] << " ";

    cout << "\n\nInitial array: ";
    for (int i = 0; i < size; i++)
        cout << arr_dob[i] << " ";
    map<double, size>(arr_dob, change);
    cout << "\nChanged array: ";
    for (int i = 0; i < size; i++)
        cout << arr_dob[i] << " ";

    cout << "\n\nInitial array: ";
    for (int i = 0; i < size; i++)
        cout << arr_char[i] << " ";
    map<char, size>(arr_char, change);
    cout << "\nChanged array: ";
    for (int i = 0; i < size; i++)
        cout << arr_char[i] << " ";*/

    return 0;
}