#include "task3.h"

int main()
{
    const size_t n = 5;
	int* arr = new int[5]{3504, 213, -465, -1450, -3000};
	map<int, n>(arr, change_mod_1000);
    for (int i = 0; i < n ; i++ )
        std::cout << arr[i] << " ";
    return 0;
}