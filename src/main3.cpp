#include <iostream>
#include "task3.h"

int main() {
    const size_t n = 5;
    int arr[n]{1, 2, 3, 4, 5};
    map <int, n> (arr, change);
    for (int i : arr)
        std::cout << i << " ";

    return 0;
}