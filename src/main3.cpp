#include<iostream>
#include "task3.h"

int main() {
    const size_t n = 10;
    int arr[n]{ 15, 10, 5, 0, -5, -10, -15, -20, 20, 25 };
    map<int, n>(arr, change);
    for (int i : arr)
        std::cout << i << " ";
    return 0;
}
