#include <iostream>
#include "task1.h"

using namespace std;

#define SIZE 5

int main()
{
/*    int arr_int[SIZE] = { 60, 83, 13, 76, 40 };
    double arr_dob[SIZE] = { 6.0, 8.3, 1.3, 7.6, 4.09 };
    char* arr_char[SIZE] = { "one", "7", "qwerty", "A4", "milk" };

    cout << "Initial array: ";
    for (int i = 0; i < SIZE; i++)
        cout << arr_int[i] << " ";
    msort(arr_int, SIZE);
    cout << "\nSort array: ";
    for (int i = 0; i < SIZE; i++)
        cout << arr_int[i] << " ";

    cout << "\n\nInitial array: ";
    for (int i = 0; i < SIZE; i++)
        cout << arr_dob[i] << " ";
    msort(arr_dob, SIZE);
    cout << "\nSort array: ";
    for (int i = 0; i < SIZE; i++)
        cout << arr_dob[i] << " ";

    cout << "\n\nInitial array: ";
    for (int i = 0; i < SIZE; i++)
        cout << arr_char[i] << " ";
    msort(arr_char, SIZE);
    cout << "\nSort array: ";
    for (int i = 0; i < SIZE; i++)
        cout << arr_char[i] << " ";*/

    return 0;
}
