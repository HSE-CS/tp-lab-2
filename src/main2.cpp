#include <iostream>
#include "task2.h"

using namespace std;

int main()
{
/*    const size_t N = 5;
    char* array_char;
    int* array_int;
    double* array_double;

    array_char = createArr<char, N> (gen);
    for (int i = 0; i < N; i++)
        cout << array_char[i] << "  ";

    cout << "\n";

    array_int = createArr<int, N> (gen);
    for (int i = 0; i < N; i++)
        cout << array_int[i] << "  ";

    cout << "\n";

    array_double = createArr<double, N> (gen);
    for (int i = 0; i < N; i++)
        cout << array_double[i] << "  ";*/

    return 0;
}